spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/porlat #ссылка на realm
      client:
        provider:
          keycloak:
            issuer-uri: http://localhost:8080/realms/porlat #ссылка на realm
            user-name-attribute: preferred_username
        registration:
          keycloak:
            client-id: springsecurity
            client-secret: aDAmgfYWYpvSdNgFjiMbRQoh54mfE6Sn
            scope: openid
server:
  port: 8081
logging:
  level:
    org.springframework.security: TRACE